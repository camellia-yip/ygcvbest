# JEPA 系列相关工作对比 + CVPR 选题方向

更新日期：2026-09-21

## 背景

JEPA（Joint Embedding Predictive Architecture）由 LeCun 提出，核心主张是放弃像素级生成式重建，转而在 latent 空间做预测，以获得更抽象、更少噪声的表征。从 I-JEPA（图像域）到 V-JEPA/V-JEPA2（视频域），这条线的演进很大程度上是在解决**表征坍缩（collapse）**问题——如何在不依赖负样本对比、也不做逐像素重建的前提下，让预测器不会学到平凡解（所有输入映射到同一个点）。V-JEPA2 通过大规模视频自监督预训练，在坍缩控制和特征质量上已经比较成熟。

近一年的趋势是把这套 JEPA 范式**迁移到交通/驾驶场景**：从纯感知预训练（AD-L-JEPA 把 JEPA 搬到 LiDAR BEV）、到直接复用 V-JEPA 架构在驾驶视频上重新预训练（Drive-JEPA/WA-JEPA）、再到把 JEPA 的 latent 用于规划任务的检索或联合动作预测（Auto-JEPA、WA-JEPA）、以及针对 JEPA 预测头本身做多模态分布改造（Branch-JEPA）。这条迁移路径提出一个值得追问的问题：**交通场景的特殊性（强交互性、多主体异质性、强多模态未来分布）反过来是否会推动 JEPA 本身的方法进步**，而不只是单向地把 JEPA 套用到一个新的下游任务？这是本文档要梳理的核心线索。

数据集是这条研究线的载体之一，目前尚未锁定；HetroD（异质交通，车/两轮车/行人混行，台湾机车密集场景）是未来可能会用到的候选数据集之一，不代表已经确定。当前先梳理已有 5 篇 JEPA 系相关工作的方法、数据集与继承关系，找出空白点，再倒推"任务/创新点"和"数据集"该如何组合。

## 继承关系总览

```
I-JEPA (2023, 图像自监督表征)
  └─ V-JEPA / V-JEPA2 (Meta, 视频自监督表征，masked latent prediction)
        ├─ AD-L-JEPA (arXiv 2501.04969)：把 JEPA 思想搬到 LiDAR BEV，预测 BEV 嵌入而非像素/体素；
        │     不直接用 V-JEPA2 权重，是"JEPA 范式在点云域的首次系统性应用"
        ├─ Drive-JEPA (arXiv 2601.22032v2)：直接复用 V-JEPA 架构/预训练范式，
        │     在驾驶视频（CoVLA+DrivingDojo+OpenScene, 330h）上重新自监督预训练，
        │     再接多模态轨迹蒸馏头
        ├─ Auto-JEPA (arXiv 2607.29031v1)：冻结现成 V-JEPA2 视觉编码器（不重新预训练），
        │     只训练新增的轨迹自编码器+意图预测器，把 JEPA 从"训练期表征学习"
        │     延伸到"推理期做检索 key"
        ├─ WA-JEPA (arXiv 2608.20974v2)：以 V-JEPA "原生"架构为基础做结构性改造——
        │     用条件流匹配替换随机掩蔽+确定性回归，是目前对 V-JEPA2 范式改动最大的一支
        └─ Branch-JEPA (arXiv 2607.05238v3)：不针对视觉模态，通用 JEPA 世界模型的
              分布头改造（有限支撑多分支替代点预测），在 Argoverse2 轨迹域验证，
              可看作与上述四篇正交的"decoder/prediction-head"层面的改进，
              可迁移到任何 JEPA-style 轨迹预测器
```

一句话区分：AD-L-JEPA 是"JEPA 搬到 LiDAR"，Drive-JEPA/WA-JEPA 是"V-JEPA2 视频编码器搬到驾驶视频再接规划头"（WA-JEPA 改动更彻底），Auto-JEPA 是"冻结 V-JEPA2 只用它的特征做意图检索"，Branch-JEPA 是"JEPA 预测头本身的多模态分布改造"，不绑定具体感知模态。

## 逐篇对比表

| 维度 | AD-L-JEPA (2501.04969) | Drive-JEPA (2601.22032v2) | Branch-JEPA (2607.05238v3) | Auto-JEPA (2607.29031v1) | WA-JEPA (2608.20974v2) |
|---|---|---|---|---|---|
| 输入模态 | LiDAR 点云→BEV | 前视驾驶视频 | 轨迹/状态（无视觉） | 视觉(冻结V-JEPA2)+轨迹历史+指令 | 视频(nuPlan) |
| 与V-JEPA2关系 | 独立提出JEPA-LiDAR范式，非直接继承 | 复用V-JEPA架构，驾驶域重新预训练 | 不涉及视觉编码器，改的是预测头/分布 | 直接冻结使用V-JEPA2权重做特征提取 | 以V-JEPA为基座做masking/预测目标的结构性改造 |
| 任务 | 3D目标检测(预训练) | 端到端规划(NAVSIM/Bench2Drive) | 轨迹世界模型/多模态后继预测 | 端到端规划(检索式) | 世界-动作联合建模/规划 |
| 数据集 | KITTI3D, Waymo, ONCE | CoVLA+DrivingDojo+OpenScene(预训练,330h)→NAVSIM v1/v2, Bench2Drive(评测) | Argoverse2(主), OGBench/AntMaze(迁移验证) | NAVSIM v1(110k轨迹记忆库)/v2 | nuPlan(预训练)→NAVSIM微调, NAVSIM-v2/HUGSIM评测 |
| 多模态未来处理 | N/A(检测任务) | 32条规则模拟器轨迹提案+蒸馏 | 有限支撑多分支+能量分数目标 | 意图latent→300条轨迹检索+场景评分门禁 | 条件流匹配覆盖latent未来分布 |
| 关键结果 | ONCE 500K帧 +2.98mAP,训练成本降1.9-2.7x GPU时 | NAVSIM v1 93.7 PDMS(SOTA), v2 87.8 EPDMS(SOTA) | AV2能量分数改进5.8-6.5%,概率加权距离9.3-10.4% | NAVSIM v1 91.3 PDMS, v2 89.1 EPDMS | NAVSIM-v2 91.7 EPDMS(+1.6超最强基线), HUGSIM HD-Score 0.4462 |
| 局限 | 未详述局限 | 依赖模拟器伪标签,仅前视,32提案可能有天花板 | 分支非唯一性,K倍解码成本未讨论 | 记忆库覆盖有限,门禁在极端场景不可靠 | 依赖特定预训练数据集效果 |

## 共同空白（从 CVPR 审稿委员会视角看）

假设这五篇论文是同一届 CVPR 同时送审的一组稿件，AC/审稿人横向比较后大概率会共同点出以下问题——这些不是"作者没想到"的空白，而是评审最容易写进 weakness 里、也是我们论文最应该主动回应或超越的点。

### 逐篇：审稿人大概率会问什么

| 论文 | 审稿人可能的质疑 |
|---|---|
| AD-L-JEPA | 只在检测这一个下游任务验证，"预训练更省算力"是否只是因为对比对象（Occupancy-MAE）本身效率不高？没有和其他非生成式点云预训练方法（如对比学习类）做统一预算下的公平对比 |
| Drive-JEPA | 用规则模拟器生成的轨迹当蒸馏"伪标签"，再在同样带规则评分逻辑的 NAVSIM 上评测，存在**评测目标和监督信号同源**的循环论证风险；仅前视单摄像头，Bench2Drive 220 条路线规模偏小，闭环结论外推力弱 |
| Branch-JEPA | 有限支撑多分支头相对于已有的 anchor-based 多模态头（如 MTR、Wayformer 里常见的锚点+分类）新意有多大？K 倍解码的计算/显存成本完全未报告，审稿人会要求公平算力预算下的对比；OGBench/AntMaze 是 RL 基准而非真实交通场景，外部效度存疑 |
| Auto-JEPA | 检索式方案高度依赖轨迹记忆库覆盖率，论文未说明记忆库规模-性能的 scaling 关系；"门禁机制"是启发式规则还是可学习模块不够清楚，可靠性证据不足；NAVSIM 是非反应式（non-reactive）仿真，规划类论文常被质疑"闭环"叙事名不副实 |
| WA-JEPA | +1.6/+1.3 个百分点的提升是否经过多次随机种子验证、是否统计显著未说明；掩蔽策略改动、流匹配、联合未来-动作预测器三处改动叠加，缺少逐项隔离的消融，无法确认增益具体来自哪一部分 |

### 五篇共同会被点出的问题

1. **单一/少数数据集验证，缺乏跨数据集鲁棒性证据**——每篇都只在 1-2 个数据集上训练和评测，审稿人常规追问"这个结论换个城市/换个数据源还成立吗"。
2. **改进幅度的统计显著性汇报不一致**——只有 Branch-JEPA 明确给了多 seed 置信区间，其余四篇多是单次跑分对比 SOTA，容易被要求补充方差/显著性检验。
3. **"闭环"能力普遍被弱化验证**——NAVSIM 系评测本质是非反应式仿真，Bench2Drive/HUGSIM 覆盖路线有限，"世界模型/规划"这类强叙事和实际闭环证据之间有落差，这也是安全关键应用类稿件常见的审稿痛点。
4. **消融未能真正隔离各改动的独立贡献**——尤其 WA-JEPA、Drive-JEPA 这种多模块叠加的方法，审稿人会要求逐项 ablation 而非只有"完整模型 vs baseline"。
5. **算力/延迟对比缺失**——没有一篇在同等参数量、同等训练预算下公平比较方法间的效率-精度权衡（Branch-JEPA 的 K 倍解码尤其明显）。
6. **少数但安全关键的主体类别未单独评估**——五篇论文的评测场景仍以机动车为主，两轮车/行人这类小样本但高风险类别没有单独的分项指标，这在强调"安全"的驾驶论文里是常见的 weakness 意见。
7. **"reasoning"/"world model"命名与实际验证方式不匹配**——latent 被称作 reasoning 或 intent，但没有一篇提供因果/反事实层面的验证（只做端到端性能对比），容易被要求"证明这个 latent 真的在做你说的那件事，而不只是又一个特征向量"。

这些问题直接对应前面"候选创新点方向"里的 A（异质主体联合建模，回应第6点）、B（可诊断 reasoning，回应第7点），也提示如果走 A/B 路线，论文里应主动补上第1-5点（跨数据集或至少跨子集鲁棒性、多 seed 显著性、消融隔离、算力对比、闭环表述的边界声明），把审稿人本会挑出的问题变成我们论文里已经交代清楚的部分。

## 候选创新点方向（不绑定数据集）

**方向 A：异质多主体联合 JEPA 世界模型**
把 Branch-JEPA 的有限支撑多分支预测，从"单 agent 多模态"扩展到"多异质 agent 联合多模态"——预测头同时对车、两轮车、行人输出带交互耦合的分支分布，而非逐个体独立预测再拼接。
- 创新点清晰：现有 JEPA 系工作都是单 agent；联合异质分布在几何/动力学上本质更难（不同类型速度/尺寸量纲不同）。
- 数据集要求：需要密集异质交互、多类型主体都有轨迹标注——HetroD、INTERACTION、SinD、CitySim 都满足；NAVSIM/AV2/Waymo 不太行（背景多为车辆稀疏标注）。

**方向 B：可诊断的 interaction-latent reasoning（因果/反事实）**
把 JEPA 预测的 latent 显式设计成"关键主体识别 + 他人未来"的中间表示，支持反事实移除/替换关键主体来验证 reasoning 是否真正影响下游运动生成，而不是只报 ADE/FDE。
- Auto-JEPA 的检索式设计是个可借鉴的起点（检索 key 天然可换）。
- 数据集要求：不强依赖异质性，但异质交互密集的场景（HetroD/INTERACTION）更容易挑出"必须靠 reasoning 才能预测对"的样本，是更有说服力的 testbed。

**方向 C：分布头选型研究（有限支撑 vs 流匹配）在异质速度分布下的系统对比**
系统对比 Branch-JEPA(有限支撑) 和 WA-JEPA(流匹配) 在"类型内单峰(汽车) vs 类型间多峰/长尾(两轮车)"场景下谁更好，本身就是一个有意思的实证贡献。
- 相对"轻"一些的创新（更像系统性实证研究而非新方法），要想中 CVPR，需要新方法或强 insight 支撑，不能只是跑分。

**方向 D：跨数据集/跨地域 JEPA 世界模型泛化**
在多个数据集（如 HetroD + NAVSIM/AV2 + INTERACTION）上预训练/评估同一 JEPA 世界模型，研究地域（台湾机车 vs 欧美车辆为主）、密度、主体构成差异下的迁移能力。
- 卖点：目前没人做过 JEPA 世界模型的跨异质交通域泛化研究；但工程量最大（要打通多个 schema）。

### 初步判断

A 和 B 组合（异质联合分布 + 可诊断 reasoning）新意最强，且这一组合恰好呼应了"背景"里提出的问题——交通场景的强交互性/多主体异质性可能倒逼 JEPA 本身（预测头的分布形式、latent 的可诊断性）做出方法层面的改进，而不只是套用；C 更适合作为 A/B 论文里的消融/分析小节而非主贡献；D 适合作为"A/B 做完还有余力"的扩展实验或独立附加故事线。**尚未最终决定，待进一步讨论确认。**

## 若选 HetroD 作为（候选）数据集时的具体衔接点

以下要点假设未来选用 HetroD，供参考，不代表已锁定：

1. HetroD 无逐帧信号相位、无 LiDAR/视频原始数据（是轨迹+vectorized map），因此 AD-L-JEPA（LiDAR）、Drive-JEPA/WA-JEPA（原始视频预训练）的"重新自监督预训练视觉编码器"路线不直接适用——更接近 Branch-JEPA 的轨迹域 JEPA 世界模型，或 Auto-JEPA 的"轨迹自编码器+意图 latent"思路。
2. Branch-JEPA 的"有限支撑多分支预测头"对应方向 C 的比较对象，也对应 HetroD 任务定义里悬而未决的"ADE/FDE best-of-K 是否需要换成 proper scoring rule（Energy Score）"这一待验证项。
3. Auto-JEPA"预测的未来 latent 当作检索 key"的思路，对方向 B（反事实诊断）有直接参考价值：检索式设计天然可解释、可做反事实（替换检索池/mask 关键主体看检索结果变化）。
4. WA-JEPA"未来导向掩蔽 + 联合未来-动作预测器"证明了在统一 latent 空间里同时对"世界"和"动作/轨迹"建模可行，可作为方向 A/B 消融设计的对照组参考。
5. AD-L-JEPA 说明 JEPA 范式（方差正则化防坍缩、预测嵌入而非像素/点）在检测这类判别任务上也能省算力——如果后续要做感知预训练（目前 HetroD 任务是纯轨迹+地图输入，暂不涉及），是一个可迁移的参考点，但当前任务定义下优先级低于轨迹/世界模型端的三篇。
6. 五篇论文均未处理"车/两轮车/行人异质主体+高密度混行"，这正是方向 A 的核心论据来源，HetroD 在这一点上数据条件成熟。

## Sources

- [Branch-JEPA (2607.05238v3)](https://arxiv.org/html/2607.05238v3)
- [Auto-JEPA (2607.29031v1)](https://arxiv.org/html/2607.29031v1)
- [Drive-JEPA (2601.22032v2)](https://arxiv.org/html/2601.22032v2)
- [WA-JEPA (2608.20974)](https://arxiv.org/abs/2608.20974)
- [AD-L-JEPA (2501.04969)](https://arxiv.org/abs/2501.04969)
